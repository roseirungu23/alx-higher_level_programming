#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    for num in range(len(my_list)):
        if num % 2 == 0:
            multiples.append(True)
        else:
            multiples.append(False)
        return multiples
